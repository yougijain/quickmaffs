# Cloud sync is OPTIONAL. Leave these unset to run in local-only mode
# (all features work; scores stay on-device). Set both to enable Supabase
# auth + cross-device sync.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
